# HTTP API

The canonical, machine-readable spec is at <https://append.page/api/spec.json> (also at [`server/src/app/api/spec.json/route.ts`](../server/src/app/api/spec.json/route.ts)).

The human-readable narrative version is in [`AGENTS.md`](../AGENTS.md) §6.

This file is here so people grepping `docs/` find a pointer to the source of truth.
